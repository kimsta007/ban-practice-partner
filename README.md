# BAN Practice Partner

Recruiting landing page for Behavior Analyst Network Practice Partners.
React + Vite + Tailwind CSS v4, no backend.

## Running it

```bash
npm install
npm run dev      # dev server on http://localhost:5173
npm run build    # static output in dist/
npm run preview  # serve dist/ locally to check the build
```
